# crypto-intel-mcp configuration — copy this file to `.env` and fill in what you have.
#
# The example source needs no keys. Add the keys your real sources require, e.g.:
# SOME_API_KEY=your-key-here
